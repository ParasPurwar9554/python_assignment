# Python Assignment 3

This assignment contains two simple Python tasks:

- **Task 1:** Defines a function named factorial that takes a number as an argument and calculates its factorial using a loop or recursion.
- **Task 2:** Uses the math module to calculate the:
    o   Square root of the number
    o   Natural logarithm (log base e) of the number
    o   Sine of the number (in radians)

---

## 📝 Task 1 - Factorial of a Number

### Description
defines a function named factorial() that takes a number as input and calculates its factorial using recursion.

### Functionality
1. Takes integer input from the user..
2. Checks whether the number is positive..
3. Calculates factorial using recursion:
   Base case → returns 1 if number == 1.
   Recursive case → returns number * factorial(number − 1).
4. Displays the final factorial value.

---

## 🧮 Task 2 - Math Module Operations

### Description
This Python program demonstrates the use of the math module to perform three operations on a user-input number:
  Square root
  Natural logarithm (log base e)
  Sine (in radians)

### Functionality
1. Imports the math module.
2. Takes a floating-point number as input..
3. Validates that the input is positive.
4. Calculates:
     Square root using math.sqrt()
     Natural logarithm using math.log()
     Sine using math.sin()
5. Uses try-except for error handling.     
6. Displays all results neatly.

---
## ▶️ Example Output

## Example Run

---

## How to Run
1. Clone or download the repository.  
2. Open terminal or command prompt in the project directory.  
3. Run the Python scripts:
```bash
python task1.py
python task2.py

